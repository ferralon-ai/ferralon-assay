package com.example.app;

// Minimal Java source so the tree is recognized as a Java checkout. The disqualification
// (version-range) axis reads pom.xml; no sink/reachability claim is made for this advisory.
public class App {
    public static void main(String[] args) {
        System.out.println("dep-repro");
    }
}
