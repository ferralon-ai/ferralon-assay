# last 24h all high critical

Precomputed `-advisory-corpus` root: advisories **published within the last 24h** of the snapshot's latest data (t0 = 2026-08-07T17:16:13+00:00), ecosystem = **all languages**, severity = **critical/high**.

Severity is the OSV `database_specific.severity` label; records without one are `unspecified` and are excluded from a severity-scoped policy rather than guessed. Windows key on the advisory's OSV **published** date.

Freshest slice. Recently-published advisories are not yet symbol-enriched, so this policy is expected to carry **no** symbol-bearing records — reachability has nothing to resolve here yet, by design of the snapshot.
