// Program.cs holds the fixture's top-level statements. functions (§4.3 row 3): Run is
// a module-scope callable with NO declaring type — the C# top-level-statement analog of
// a free function (Enclosing = ""), which is exactly what separates a function from a
// static method (SYMBOLS.md §Kind). Read lexically only; never built or run.
void Run()
{
}

Run();
